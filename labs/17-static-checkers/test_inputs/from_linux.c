foo() {
	if (foo) {
		goto close_fds;
	} else {
        }
close_fds:
        return;
}

bar() {
    redirect = (ifp && ifp->flags);
    if (ifp)
        in6_ifa_put(ifp);
}

baz() {
    goto unlock;

    queue_for_each_hw_ctx(foo)
    {
        break;
    }

unlock:
    return;
}
